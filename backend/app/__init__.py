# Backend ProtoCRM (Fase C/D) — FastAPI, misma API que el mock/n8n.
# Uso:
#   pip install -r backend/requirements.txt
#   python -m uvicorn backend.app.main:app --port 8000
#   Abrir index.html con Base = http://localhost:8000
# Lee el .env de la raíz del proyecto (WA_*, OPENROUTER_*, DATABASE_URL, JWT_SECRET).
